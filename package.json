{
  "name": "10x-marketplace",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "sync:check": "node scripts/sync-plugins.mjs --check",
    "sync:write": "node scripts/sync-plugins.mjs --write",
    "validate": "node scripts/validate-marketplace.mjs"
  }
}
